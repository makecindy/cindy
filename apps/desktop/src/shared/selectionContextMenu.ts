import { IPC_CHANNELS } from '@cindy/cindy-ipc';

/** Main → renderer command emitted when the native menu chooses Add to chat. */
export const SELECTION_CONTEXT_MENU_ADD_TO_CHAT_CHANNEL = IPC_CHANNELS.SELECTION_CONTEXT_MENU.ADD_TO_CHAT;
