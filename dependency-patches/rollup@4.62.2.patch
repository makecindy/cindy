diff --git a/dist/es/shared/node-entry.js b/dist/es/shared/node-entry.js
index 0c67f4b0befbb8af05adc5c4b0216cacbf8c169a..c98cae830f2ae944c4fb47592bd5e408e85fcef4 100644
--- a/dist/es/shared/node-entry.js
+++ b/dist/es/shared/node-entry.js
@@ -7226,7 +7226,7 @@ const copyOwnPropertyLiveBinding = (t, index, { _, cnst, getDirectReturnFunction
         name: null
     });
     return (`${index}${cnst} d${_}=${_}Object.getOwnPropertyDescriptor(e,${_}k);${n}` +
-        `${index}Object.defineProperty(n,${_}k,${_}d.get${_}?${_}d${_}:${_}{${n}` +
+        `${index}Object.defineProperty(n,${_}k,${_}d${_}&&${_}d.get${_}?${_}d${_}:${_}{${n}` +
         `${index}${t}enumerable:${_}true,${n}` +
         `${index}${t}get:${_}${left}e[k]${right}${n}` +
         `${index}});${n}`);
@@ -16953,6 +16953,14 @@ function resetSourcemapCache(map, sourcemapChain) {
     }
 }
 function decodedSourcemap(map) {
+    // Watch mode can pass a sourcemap wrapper from the previous graph generation
+    // back through the cache. Re-wrapping it reads the lazy mappings accessor,
+    // copies the decoded array, and leaves the old array retained by the cached
+    // module. Reuse the wrapper so resetSourcemapCache can release its decoded
+    // cache without creating a second large array.
+    if (map && typeof map === 'object' && sourceMapCache.has(map)) {
+        return map;
+    }
     if (!map)
         return null;
     if (typeof map === 'string') {
diff --git a/dist/shared/rollup.js b/dist/shared/rollup.js
index 30daac96da9300d5d1e60b3df9143c2a37083741..999b3e1a3d66fb7e71ab7154c3f4289379372649 100644
--- a/dist/shared/rollup.js
+++ b/dist/shared/rollup.js
@@ -11323,7 +11323,7 @@ const copyOwnPropertyLiveBinding = (t, index, { _, cnst, getDirectReturnFunction
         name: null
     });
     return (`${index}${cnst} d${_}=${_}Object.getOwnPropertyDescriptor(e,${_}k);${n}` +
-        `${index}Object.defineProperty(n,${_}k,${_}d.get${_}?${_}d${_}:${_}{${n}` +
+        `${index}Object.defineProperty(n,${_}k,${_}d${_}&&${_}d.get${_}?${_}d${_}:${_}{${n}` +
         `${index}${t}enumerable:${_}true,${n}` +
         `${index}${t}get:${_}${left}e[k]${right}${n}` +
         `${index}});${n}`);
